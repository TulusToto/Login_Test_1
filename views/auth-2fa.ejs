<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Activate 2 Factor Authentication</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .container {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .card {
            border-radius: 15px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .card-body {
            padding: 2rem;
        }

        .card-title {
            color: #437ea5;
            font-size: 24px;
            font-weight: bold;
            text-align: center;
        }

        .form-label {
            font-weight: bold;
        }

        .btn-submit {
            background-color: #437ea5;
            color: white;
            transition: all 0.3s ease;
        }

        .btn-submit:hover {
            background-color: #346285;
        }
    </style>
</head>
<body>
<div class="container">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-body">
                <h4 class="card-title">Activate 2 Factor Authentication</h4>
                <form action="/auth-2fa" method="POST">
                    <p class="text-center">Scan the QR Code in the Authenticator app</p>
                    <div class="mb-3 d-flex justify-content-center">
                        <img src="<%= qr %>" class="img-fluid" alt="QR Code">
                    </div>
                    <div class="mb-3">
                        <label for="code" class="form-label">Enter Code</label>
                        <input type="text" class="form-control" id="code" name="code">
                    </div>
                    <button type="submit" class="btn btn-submit mt-3 w-100">Submit</button>
                </form>
                <a href="/home" class="btn mt-2 w-100" style="color: white; background-color: #2abe48" rel="noreferrer">Home</a>   
            </div>
        </div>
    </div>
</div>
</body>
</html>
